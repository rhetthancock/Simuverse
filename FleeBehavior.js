class FleeBehavior {
    constructor(fleeSpeed, fleeAcceleration, fleeMaxDuration) {
        this.fleeSpeed = fleeSpeed;
        this.fleeAcceleration = fleeAcceleration;
        this.fleeMaxDuration = fleeMaxDuration;
    }

    flee(npc, player) {
        // Implementation of fleeing action
    }

    checkBehind(npc) {
        // Implementation of checking behind action
    }
}
